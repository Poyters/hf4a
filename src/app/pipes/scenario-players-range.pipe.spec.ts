import { ScenarioPlayersRangePipe } from './scenario-players-range.pipe';

describe('ScenarioPlayersRangePipe', () => {
  it('create an instance', () => {
    const pipe = new ScenarioPlayersRangePipe();
    expect(pipe).toBeTruthy();
  });
});
